# Fireplace - a Vim clojure extention

* `cp` evalute motion
* `cpp` evaluate form under cursor
* `gf` goto file on namespace
* `K` look up with `doc`
* `[d` looks up symbol under cursor with `source`
